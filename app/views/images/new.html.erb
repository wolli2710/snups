<%= start_form_tag @image, :html => { :multipart => true } do |f| %>
	<%= f.label :user_id %><br/>
	<%= f.text_field :user_id %><br/>
	<%= f.label :mission_id %><br/>
	<%= f.text_field :mission_id %><br/>
	<%= file_field(:image, :nameHash)%><br/>
	<%= f.submit 'abschicken' %>
<% end %>